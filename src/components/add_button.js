import React, {Component} from 'react';

const AddButton = (props) => {
    return (
        <button className="add-button ui primary labeled icon button">
            <i className="plus icon"></i>
            Add Item
        </button>
    );
}

export default AddButton;